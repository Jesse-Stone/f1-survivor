import * as React from 'react';
import Button from '@mui/material/Button';

export default function ExampleButton() {
  return (
    <div>
      <Button variant="contained">Make Pick</Button>
    </div>
  );
}